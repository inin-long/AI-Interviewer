# AI Interviewer 开发状态

最后更新：2026-07-20

## 当前结论

MVP 业务主流程已经贯通：本地账户 → 简历解析 → 候选人画像确认 → 面试方案 → AI 面试与暂停恢复 → 后台评分 → 面试记录与报告。

当前主线进入 Windows 发布验收阶段。局部体验优化不得阻塞发布链验证，除非问题会造成数据丢失、安全风险或主流程不可用。

## 简历中心重构（2026-07-20）

- [x] 按目标设计重建简历中心：标题操作区、简历总数/已确认画像/待处理/最近更新 4 张统计卡、状态胶囊筛选、候选人搜索、`AppSelect` 排序、卡片式简历列表和底部解析流程已完整落地。
- [x] 画像工作区按最终要求改为 `470px` 右侧通用 `DrawerPane`，不再常驻占用主内容宽度；单击简历整行或“查看画像”打开，遮罩和关闭按钮均可退出。
- [x] “查看原文”复用同一 Drawer，并提供返回画像入口；详细编辑继续进入既有画像编辑页，不丢失保存草稿、重新分析和人工核对能力。
- [x] 上传/本地导入 → 后台文本解析 → 生成画像 → 确认保存 → 关联方案 → 启动或恢复模拟面试形成闭环；无关联方案时明确进入方案页补齐关联，不自动制造方案。
- [x] 搜索覆盖文件名、候选人姓名和目标岗位；筛选始终保持有效选项；排序支持更新时间、名称和文件大小；状态覆盖已确认、待确认、待生成、解析中和解析失败。
- [x] ImageGen 生成素材已落库：`images/resume/candidate-avatar.png` 用于画像头像，`images/resume/resume-sidebar-illustration.png` 为 chroma-key 去底后的透明侧栏插画；简历路由显示插画，其他页面不受影响。
- [x] JavaFX 原生 `1672 × 901` 主界面和 Drawer 截图已与参考图完成同画幅及聚焦区域比较；两轮 P2 密度问题已收敛，`design-qa.md` 最终结果为 passed。
- [x] `ResumeCenterSnapshotTest` 使用隔离数据库生成 6 种业务状态截图；`JavaFxFxmlLoadTest` 覆盖统计卡、排序、列表、流程、Drawer 宽度和生成素材；完整 `mvnw test` 共 167 项，163 项通过，4 项按条件跳过，0 失败、0 错误。

## UI 通用组件状态（2026-07-19）

### 弹窗与选择器

- [x] `AppDialog<R>`：统一无系统装饰的模态弹窗外壳，包含 AI 品牌标题栏、语义图标、标题/说明/业务内容区、固定操作区和阴影遮罩；默认宽度 480px，可复用任意 JavaFX `Node` 作为内容。
- [x] `AppDialogs`：提供文本输入、确认操作、信息/警告/错误消息四类标准配方；删除类操作使用危险色主按钮，普通确认使用品牌色主按钮。
- [x] `AppSelect<T>`：兼容原 `ComboBox<T>` API，统一默认态、悬停态、聚焦/展开态、弹出列表、当前项强调和选中勾选图标；FXML 控制器无需改变既有字段类型。
- [x] 键盘与状态：弹窗支持 `Esc` 取消、`Enter` 执行默认操作、关闭按钮和初始焦点；必填项未满足时主按钮呈明确禁用态；选择器展开时边框和箭头使用品牌色反馈。

### 视觉规范

- 品牌主色 `#4F63D9`，聚焦边框 `#7484E8`，危险色 `#C53B46`；正文、次级文字、边框继续复用 `app.css` 的全局设计令牌。
- 弹窗圆角 14px、输入与选择器圆角 9px、操作按钮圆角 8px；弹窗内容间距 16px，底部操作区采用浅灰背景与顶部分隔线。
- 选择器弹出列表单项高度 38px，最多显示 8 项；悬停使用 `-hover`，选中使用 `-primary-soft`，不再显示系统默认青色高亮和直角菜单。

### 全局滚动轴

- [x] `app.css` 已统一覆盖 `ScrollPane`、`ListView`、`TableView`、`TreeView`、`TreeTableView`、`TextArea` 和选择器弹出列表中的纵向/横向滚动轴。
- 滚动轴宽度/高度统一为 10px，轨道透明，滚动块使用 `#C7CEDA`；悬停切换为 `#AEB8C8`，按下切换为品牌聚焦色 `#7484E8`。
- JavaFX 默认增减箭头及按钮占位已移除，横纵滚动轴交汇角保持透明；滚动块采用胶囊圆角，与卡片和输入组件的轻量视觉一致。
- JavaFX 原生布局测试覆盖页面、列表、文本域的可见滚动轴，校验尺寸、滚动块背景及增减按钮折叠状态。

### 接入范围与约束

- 知识库已接入：新建分类、上传时选择分类、删除文档确认、文档排序。
- 全局错误/信息提示及后台任务、测评记录、面试记录、面试方案、面试工作区、题库、简历、职业规划、技巧文章中的确认/警告弹窗已迁移到 `AppDialogs`。
- 知识库、面试记录、面试方案编辑器、题库、题目编辑器、技巧文章编辑器和技巧库中的 FXML 选择器已迁移到 `AppSelect`。
- 上述 7 个页面共 18 个选择器的控制器字段也已收紧为 `AppSelect<T>`，不再仅以原生 `ComboBox<T>` 父类兼容声明。
- 技巧库加载失败提示中遗留的全限定 `javafx.scene.control.Alert` 已迁移为 `AppDialogs`。
- 后续业务代码不得直接创建 `Alert`、`Dialog`、`TextInputDialog`、`ChoiceDialog` 或原生 `ComboBox`；新弹窗使用 `AppDialog`/`AppDialogs`，新选择器使用 `AppSelect`。
- JavaFX FXML/组件定向测试已覆盖品牌外壳、操作按钮、取消/默认按钮语义、选择器 CellFactory 及全部页面接入；守卫同时拦截普通导入、全限定类名和控制器父类兼容声明三种原生控件回退方式。
- 弹窗关闭回归已覆盖真实 `show()` 生命周期：右上角关闭、取消按钮和 `Esc` 均依赖隐藏的 JavaFX `CANCEL_CLOSE` 语义正常退出，不再被 `Dialog` 拦截。
- 完整验证使用 JDK 21 执行 `mvnw clean test`：共 165 项，163 项通过，2 项真实 Provider 测试按开关跳过，0 失败、0 错误。

## S1 开发状态

开发分支：`feature/inin-s1`

### Phase S1-1：领域知识与主张验证

- [x] DomainPack：内置 Java 后端、全栈工程师和产品经理岗位包；启动时完成结构校验、SQLite 版本化同步与 Lucene 索引重建。
- [x] 面试方案绑定 DomainPack；创建会话时冻结知识包 ID、版本和完整快照，历史会话不受内置包升级影响。
- [x] ClaimExtractorNode、InterviewClaim 与 ClaimLedger：每轮回答先提取原子主张，严格校验 JSON Schema，失败自动修复一次后安全降级；主张按用户/会话持久化并写入版本化 Checkpoint。
- [x] ProbePlannerNode 与 QuestionRendererNode：先按主张重要度、可信度与证据缺口生成结构化追问目标和策略，再由独立 Renderer 渲染单个自然语言问题；阶段首题与主张追问分流，计划写入 V2.1 Checkpoint。

### Phase S1-2：逻辑链与证据评分

- [x] LogicChainEvaluatorNode 与 LogicGap：仅分析重要回答，拆解前提、诊断、方案、决策、行动、结果与验证；结构化输出修复失败时安全降级，严重逻辑缺口可直接成为下一轮追问目标。
- [x] EvidenceCollectorNode、EvaluationEvidence、EvidenceLedger 与评分置信度：每轮从回答、主张和逻辑链生成带 `messageId`/`claimId` 的能力证据，严格区分信号强度与置信度；无充分证据使用 `INSUFFICIENT`，失败修复一次后降级且不阻断面试；报告按证据账本评分并展示可追溯来源。

### Phase S1-3：跨轮一致性验证

- [x] ConsistencyCheckNode 与 ConsistencyIssue：每 3 轮、命中相关主张或收到澄清回答时比较本轮与历史主张；潜在差异必须先进入 `POTENTIAL`，中性澄清问题实际问出后才进入 `CLARIFIED`，后续解释仅能收敛为 `RESOLVED` 或 `CONFIRMED_CONFLICT`；禁止直接做负面人格判断，失败修复一次后安全降级。
- [x] DeferredProbe 与延迟验证调度：按主张类型、重要度、缺失证据和方案阶段，将高价值主张调度到更合适的后续阶段；一致性检查降级时自动安排后续交叉验证；调度去重、用户隔离并持久化到 SQLite 与 Checkpoint，只有目标问题成功生成后才幂等标记完成，流式中断不会误消费。

### Phase S1-4：压力控制与动态场景

- [x] PressureController：按追问策略、面试难度、方案上限与教练/正式模式控制压力阶梯；跟踪连续施压和重复目标，充分证据后停止施压，达到上限自动降压；攻击性或人格判断目标会被替换为中性澄清，压力状态写入 Checkpoint。
- [x] ScenarioEngine 与场景状态持久化：V20 为每个用户/面试保存唯一活动场景；结构化记录初始与当前变量、约束、候选人决策、因果事件、事件前后快照和结束条件，使用轮次乐观锁顺序推进，并在每次加载时重放事件检测变量时间线篡改或前后矛盾。
- [x] ScenarioDirectorNode 与候选人决策驱动的事件演进：每轮从候选人原始回答提炼原子决策和依据，只允许更新场景已声明变量，并生成具备明确因果关系的约束、资源或故障事件；导演输出严格校验且仅修复一次，隐藏信息只供内部推演，问题流完整落库后才推进场景；持续失败时关闭场景并无阻塞返回普通面试流程，状态写入 V2.7 Checkpoint。
- [x] 内置岗位场景模板与方案配置：Java 后端、全栈工程师和产品经理岗位包提供完整的角色、目标、事实、假设、隐藏信息、约束、可注入事件、能力指标与结束条件；方案可配置面试模式、Persona、压力、核验严格度和场景比例，保存时执行类型化校验；会话基于冻结的 DomainPack 快照和题量预算确定性调度场景，至少覆盖场景引入与一次决策后果，并保证单场景、隐藏信息隔离、流式成功后推进及旧 Checkpoint/场景状态兼容。
- [x] Phase S1-5：Persona 与问题质量审查：六类 InterviewerPersona 只进入问题措辞层，同一追问目标可按专业面试官、未来同事、技术负责人、架构师、故障指挥者或产品负责人表达，评分与证据提示不读取 Persona；QuestionQualityGateNode 对每个完整问题检查阶段、岗位能力、验证目标、重复、多问、答案泄露、候选人信息捏造、未声明背景、难度、无意义压力和证据可评估性，不合格草稿在展示前拦截，只重写一次后降级到可信规则模板；网络中断仍保留流式片段，模板降级不会保留伪引用；七类协作与观点修正行为被转成带原回答消息来源的 Evidence Ledger 证据并同步写入 SQLite 与 Checkpoint；面试顶部展示模式、Persona、动态压力和活动场景状态。
- [x] Phase S1-6：增强报告、分支复盘与训练闭环
  - [x] 新报告模板与 Evidence 跳转：报告固定输出面试信息、综合结论、能力评分与置信度、关键证据、主张可信度、逻辑链、压力场景、决策取舍、协作修正、一致性、优势、风险、改进、学习计划和关键问答 15 个章节；结论区分负向证据与证据不足，证据/主张/一致性项标注 ID 和 Q 编号；报告 DTO 将证据消息解析为来源题号，侧栏可直接跳转对应问答。
  - [x] Session Branch 与 Branch Comparison：V21 以用户、原会话、原问题和原 Checkpoint 为边界保存可嵌套分支；报告证据可创建“重答”子页面，新回答只针对冻结上下文重新执行主张、逻辑链、证据与协作分析，生成逻辑完整度、证据数量/质量分、原/新追问、观点修正和缺口解决的本地对比报告；分支失败可重试，完成后不可覆盖，原消息、Checkpoint、会话与报告始终保持不变。
  - [x] 专项训练建议与方案创建：将负向/不足证据按能力聚合为带证据 ID 与 Q 编号的训练主题，从历史 Checkpoint 汇总逻辑缺口并生成结构化重答练习；优先关联本次会话冻结且当前可用的知识文档，并补充名称/分类匹配的用户私有资料；报告侧栏展示建议且可一键创建独立教练模式方案，方案固定轻松压力、标准核验和 0% 场景，保存来源会话、证据、缺口及教练提示规则，原方案保持不变。

### S1 完成度审计补齐

- [x] InterviewCoverage、InterviewStrategy 与 DecisionValidatorNode：会话从冻结 DomainPack 初始化岗位能力覆盖，逐轮按证据数量、置信度和覆盖程度更新并持久化；Probe Planner 优先选择高重要度覆盖缺口，策略快照记录阶段、目标、压力、剩余题量与场景轮次；最终校验补齐无目标追问、非法/提前结束、方案压力上限和连续施压上限，Checkpoint 升级至 V2.9 并兼容 V2.8。
- [x] 模式化面试工作区：正式模式保持无即时缺失项、提示和重答；教练模式在回答后展示已覆盖内容、缺失内容、逻辑缺口、参考结构和按需提示，提示服务全程只读且不写消息/证据，重新组织回答会暂停原会话并进入隔离 Session Branch；情境沙盘展示公开目标、角色、事实、约束、变量、最近事件和候选人决策，绝不读取隐藏信息；顶部显示剩余计划时长，已校验策略将剩余题量和时间共同交给问题渲染器。
- [x] S1 评分闭环审计：最终模型不再读取完整问答或返回分数，只基于 Evidence/Consistency Ledger 生成叙述；程序按逐轮正向/负向证据、强度、置信度和已确认矛盾确定固定六维分数，仅有 `INSUFFICIENT` 或无证据时维度显示“证据不足”且不作能力结论；综合分只汇总已有决定性证据的维度，分数与置信度独立保存；每个实际评分关联 evidenceId、messageId、claimId 和 Q 编号，报告表格展示来源，点击有证据的分数可跳转对应问答，旧版已完成报告仍兼容读取。
- [x] 报告判断全链路追踪：Provider 省略或提供无效 `relatedClaimIds` 时，仅以同一回答已落库主张作为安全回填；缺少主张关联的证据不得形成分数、优势或风险判断；报告中的评分、优势和风险统一展示 evidenceId、messageId、claimId 与 Q 编号，证据定位按钮显示短 ID 并可进入原问答。
- [x] 专项复试评分变化：专项训练会话继续使用方案快照中的来源面试 ID 建立不可变关联；原报告自动发现同一用户已完成的复试，分别比较有证据支撑的综合分和固定能力维度，任一侧证据不足时不制造分差；报告侧栏展示训练前后分数、维度变化并可进入复试报告。

最近一次 S1 验证（2026-07-17）：S1-1 至 S1-6 及完成度审计补齐项已全部实现；DomainPack、Claim/Evidence Ledger、结构化追问、重要回答逻辑拆解、逐轮能力证据、证据不足语义、评分置信度、报告证据来源、相关主张/周期调度、`POTENTIAL → CLARIFIED → RESOLVED/CONFIRMED_CONFLICT` 生命周期、中性澄清、重要主张延迟验证、降级交叉验证、调度幂等完成与用户隔离、证据驱动压力阶梯、连续/重复施压降级、攻击性话术替换、结构化场景状态、决策与事件因果关联、变量时间线重放校验、候选人决策驱动的动态事件、完整内置场景模板、方案级模式/Persona/压力/严格度/场景比例配置、冻结快照与题量预算驱动的场景调度、六类 Persona 措辞与评分隔离、逐题质量门、单次重写与可信模板降级、七类协作行为证据、面试状态可视化、15 章节增强报告、判断来源 ID/Q 编号、证据侧栏跳转、原题重答、逻辑/证据/追问/修正/缺口对比、原会话不可变、负向/不足证据训练主题、Checkpoint 逻辑缺口练习、知识资料关联、一键教练方案、专项复试前后评分变化、隐藏信息隔离、问题成功落库后推进、导演失败自动退出场景、结构化输出修复/降级、Checkpoint V1/V2.0/V2.1/V2.2/V2.3/V2.4/V2.5/V2.6/V2.7/V2.8→V2.9 升级、本地与真实 Provider TestFX 均通过；数据库迁移基线为 V21。

最近一次覆盖度与决策校验验证（2026-07-17）：使用 `D:\Libs\Java\jdk-21.0.2` 执行干净定向测试及完整 `mvnw test` 均通过；Surefire 145 项（143 通过、2 个真实 Provider 测试跳过），覆盖 DomainPack 初始化、证据累计、能力缺口优先级、无目标追问修复、提前结束拦截、压力上限、V2.8 迁移、V2.9 状态往返和 Agent 集成持久化。

最近一次模式化工作区验证（2026-07-17）：使用 `D:\Libs\Java\jdk-21.0.2` 执行教练反馈业务集成测试、19 个 FXML 资源检查、10 个 JavaFX/Spring FXML 加载测试及完整 `mvnw test` 均通过；Surefire 146 项（144 通过、2 个真实 Provider 测试跳过）。已验证教练反馈按最新回答消息隔离、读取前后 Evidence Ledger 数量不变、正式模式无反馈、FXML 事件与 Spring 依赖可加载，以及策略剩余时长进入渲染上下文。

最近一次证据评分闭环验证（2026-07-17）：使用 `D:\Libs\Java\jdk-21.0.2` 执行完整 `mvnw test` 通过，Surefire 149 项（147 通过、2 个真实 Provider 测试跳过）；本地 `CompleteBusinessFlowE2ETest` 通过注册登录、简历/画像、知识库、方案、三轮 Agent 面试、逐轮证据、程序汇总评分、报告与任务删除全流程。测试明确覆盖正/负证据计分、仅证据不足不计分、低置信度展示、相关已确认矛盾扣分、模型分数字段被忽略、最终模型不接收完整问答、六维证据来源，以及点击评分跳转原问答。

最近一次报告判断追踪验证（2026-07-17）：使用 `D:\Libs\Java\jdk-21.0.2` 执行 Evidence Ledger 与评分聚合定向测试通过（5 项）；已验证无效主张 ID 被过滤、空主张关联按同轮已落库主张回填、跨用户访问拒绝、未关联主张的正向证据不计分，以及优势/风险来源包含证据、消息、主张和题号。

最近一次专项复试评分变化验证（2026-07-17）：使用 `D:\Libs\Java\jdk-21.0.2` 执行训练进展与 JavaFX/Spring FXML 加载定向测试通过（12 项）；已验证仅匹配同一来源面试且已完成的专项训练、综合分与可比较维度差值、证据不足不生成虚假变化、字符串/数值来源 ID 兼容，以及报告页新增依赖和交互可加载。

最近一次 develop 迁移冲突修复（2026-07-17）：S1 迁移固定使用 `V15–V21`，题库、职业测评、技巧库与职业规划迁移顺延为 `V22–V27`，解决分支合并后 Flyway 重复版本导致的首次启动失败；新增版本唯一性回归测试，并验证全新数据库可完整迁移至 V27、已有 S1 V21 数据库可顺序升级至 V27。完整 `mvnw test` 共 154 项（152 通过、2 个真实 Provider 测试按开关跳过）。

## 主流程状态

- [x] 本地账户、SQLite 迁移和用户数据隔离
- [x] 简历上传、后台解析、画像生成与人工确认
- [x] 知识文档处理、Embedding、Lucene 用户索引和会话范围冻结
- [x] 面试方案、画像/知识关联和会话快照
- [x] 流式 AI 面试、Checkpoint、暂停恢复和失败内容保留
- [x] 最终回答幂等保存、后台报告生成、自动重试和重启恢复
- [x] 面试记录、完整问答、引用定位和 Markdown 报告
- [x] 全局任务中心、任务生命周期反馈、失败重新排队和终态任务安全删除
- [x] 本地确定性 TestFX：注册登录 → 简历解析 → 画像生成确认 → 知识解析/Embedding/Lucene → 三题 Agent 面试/RAG 引用 → 六维评分报告
- [x] 真实 Provider TestFX：使用环境变量完成真实画像、Embedding、语义检索、单题流式面试和评分报告
- [x] 真实画像专项测试：完整全栈简历经后台任务、JSON 流式 API 与落库链路；单次 Provider 尝试可观测
- [x] JaCoCo 覆盖率报告与最低门槛：总体行覆盖率不低于 70%，分支覆盖率不低于 45%
- [x] `jpackage` app-image、便携 ZIP、EXE/MSI 构建脚本
- [x] 当前代码构建 app-image，并通过首次启动与同目录重启数据保留冒烟
- [x] 当前代码实际生成开发版 EXE/MSI，并生成 SHA-256 校验清单
- [ ] 在全新 Windows 用户环境执行安装、快捷方式、卸载和升级保留数据验收
- [ ] 发布前补充 ICO 与代码签名；开发阶段不阻塞主流程

## 局部优化清单（不阻塞当前主线）

### 任务与通知

- 任务中心增加类型/状态筛选、搜索和历史任务归档。
- 连续完成多个任务时合并活动回执，并提供可回看的通知历史。
- 报告生成完成回执直接进入业务报告，而不先经过任务详情。
- 任务详情展示业务对象名称，例如简历名、知识文档名和面试方案名。

### 页面体验

- 首页替换静态能力说明，展示真实的简历、方案、面试和报告统计。
- 将简历画像页面的定时轮询统一替换为任务事件订阅。
- 为长列表补充分页或虚拟滚动策略，并统一保留筛选条件。
- 继续完善键盘操作、焦点可见性、读屏标签和最小窗口缩放。
- 在主流程稳定后补齐暗色主题。

### 组件与一致性

- 抽取任务类型、面试状态等 UI 文案映射，减少 Controller 内重复 `switch`。
- 将 Loading、Empty、Error 状态进一步统一为可复用组件。
- 统一表格状态标签、危险操作确认文案和非模态成功反馈。
- 根据最终 ICO 调整应用品牌标识和安装器视觉。

### 工程与测试

- 降低预期异常测试产生的堆栈日志噪声。
- 继续补充 UI 控制器的错误、取消、删除、暂停恢复和失败重试分支；当前控制器行覆盖率为 61.9%。
- 对真实 Provider 增加 429 限流和持续网络中断验收；当前已覆盖 DNS/超时根因诊断、凭据脱敏和后台任务重试。
- 发布前在干净 Windows 账户完成安装器升级/卸载人工验收记录。

## 明确延期

V1 不实现云同步、OAuth、语音、OCR、本地模型、代码运行、自动更新和多 AI Provider。除非产品范围变更，不应提前占用当前主线开发时间。

## 验证基线

- 开发构建：`mvnw clean verify`
- 本地 TestFX：`mvnw test-compile "-Dit.test=CompleteBusinessFlowE2ETest" failsafe:integration-test failsafe:verify`
- 真实 Provider TestFX：设置 `AI_LLM_LIVE_TEST=true` 后运行 `mvnw test-compile "-Dit.test=LiveProviderBusinessFlowE2ETest" failsafe:integration-test failsafe:verify`
- 真实画像专项测试：设置 `AI_LLM_LIVE_TEST=true` 后运行 `mvnw "-Dtest=LiveCandidateProfileApiIntegrationTest" test`
- 覆盖率报告：`target/site/jacoco/index.html`
- app-image：`packaging\windows\Build-Package.ps1 -Type app-image`
- 发布冒烟：`packaging\windows\Test-AppImage.ps1`
- 真实 AI 测试：仅在显式设置 `AI_LLM_LIVE_TEST=true` 时运行

最近一次发布验证（2026-07-13）：app-image 构建成功；首次启动完成数据库迁移并启动 2 个后台 Worker；使用同一 `AI_INTERVIEWER_HOME` 重启成功，数据库与外部用户文件保持不变；开发版 EXE/MSI 均已生成并写入 SHA-256 清单。

最近一次本地业务流程验证（2026-07-15）：TestFX 使用临时用户目录、配置式简历/知识路径、确定性 AI 与 Embedding，实现注册登录、全栈简历解析、画像生成确认并返回简历中心、知识切片与 Lucene 索引、语义检索、画像/知识会话快照、最大化面试、对话自动滚动、合法阶段切换、追问、两轮 RAG 引用、三题问答、报告验收和终态任务删除；不访问真实 Provider。

最近一次真实 Provider 验证（2026-07-15）：定位到 Spring AI OpenAI 适配器默认 3 次内部重试与后台任务 3 次重试叠加，旧版 60 秒超时会将一次画像操作放大为最多 12 次 HTTP 尝试；同时 `deepseek-ai/DeepSeek-V4-Pro` 默认思考模式在完整简历画像请求中超过 5 分钟。修复后 SDK 内部重试关闭，SiliconFlow DeepSeek V4 默认关闭思考，完整全栈简历专项测试通过；测试用例耗时 45.7 秒（Maven 总耗时约 65 秒），画像由单次 Provider 请求成功生成并落库。

最近一次 S1 真实 Provider 验证（2026-07-17）：使用环境变量中的 SiliconFlow 配置执行 `LiveProviderBusinessFlowE2ETest` 通过（132.4 秒）；真实完成账户、简历与画像、Embedding、知识检索、单题流式面试、逐轮 Claim/Evidence 和后台报告。单题证据不足时报告不制造分数，UI 明示“证据不足”，同时维度证据仍完整关联 messageId、claimId 和 evidenceId。

最近一次稳定覆盖率基线（2026-07-17）：使用 `D:\Libs\Java\jdk-21.0.2` 执行 `mvnw clean verify` 通过；Surefire 152 项（150 通过、2 个真实测试跳过），Failsafe 2 项（本地 TestFX 通过、真实 TestFX 跳过）。总体行覆盖率 83.0%、分支覆盖率 56.9%，均高于 70%/45% 门槛；全部 21 个 Flyway 迁移、19 个 FXML 资源和 10 个 JavaFX/Spring FXML 加载用例通过。真实 Provider 测试不计入稳定覆盖率基线，已另行显式启用并通过。


## 自测问题记录

1. [x] 后台任务无法删除：已支持从任务列表和详情页逻辑删除已完成/失败任务；排队中和执行中的任务禁止删除，避免业务状态残缺。

## 面试方案工作区重构（2026-07-20）

- [x] 方案列表重构为卡片工作区：支持名称/JD/重点搜索、画像与知识分类筛选、更新时间/名称/时长排序、右侧实时预览、复制、编辑、删除、导入、导出和启动面试。
- [x] 新建/编辑页补齐闭环：基础信息、简历画像、领域包、知识分类、JD、关注维度、规则参数及阶段蓝图集中配置；阶段支持启停、说明、题量、时长和权重，并校验题量/时长总量及 100% 权重。
- [x] 新增方案详情页：展示基础信息、阶段流程、关联画像与知识范围、重点维度、规则、使用记录、完整度、适用场景、风险和优化建议，可直接复制、导出、编辑、启动或删除。
- [x] 方案图标改为用户上传：编辑页支持选择、替换和移除 PNG/JPG/JPEG，保存时复制到当前用户隔离的 `plan-assets` 目录，限制 5 MB；没有上传时使用中性的内置占位图，不使用预设技术栈封面。
- [x] 方案导入/导出使用版本化 JSON；导出不携带本机绝对图标路径，导入后生成独立副本并保留分类、画像、领域包、规则和阶段配置。
- [x] 生产占位素材由内置 ImageGen 生成并保存到 `src/main/resources/images/plan/plan-placeholder.png`；Java/Spring 图片只放在 `src/test/resources/plan-icons` 模拟“用户已上传图标”的截图状态，不进入生产默认素材。
- [x] JavaFX 原生视觉验收完成：列表、编辑、详情三屏均以 `1672 × 901` 画幅进行四轮截图对比；已修复图标异步空白、编辑页阶段配置缺失和页面密度问题，最终无 P0/P1/P2 遗留。
- [x] 自动化回归：`LocalFileStorageServiceTest` 与 `JavaFxFxmlLoadTest` 共 22 项通过；使用 JDK 21.0.2 执行完整 `mvnw test`，169 项测试中 164 项通过、5 项按条件跳过，0 失败、0 错误。

视觉证据：`C:\Users\35975\.codex\visualizations\2026\07\18\019f7443-ad60-7703-91ed-cad90a055040\plan-workspace-v4.png`、`plan-editor-v4.png`、`plan-detail-v4.png` 及对应 `compare-*-v4.png`。Browser 插件不适用于 JavaFX 桌面应用，本轮使用 JavaFX 原生 `Parent.snapshot`、FXML 加载测试与控制器交互回归。

### 面试方案窄窗口与知识分类选择修复（2026-07-20）

- [x] 方案卡片宽度跟随 `ListView` 可视区，标题、元数据和重点区域允许收缩，操作区保留稳定宽度；`1366 × 846` 窄窗口下无横向滚动、无右侧预览遮挡，方案名称和主要动作完整可见。
- [x] 顶部搜索、筛选、排序和主操作收敛固定宽度，右侧预览栏及编辑/详情摘要栏降低最小宽度，避免较窄桌面窗口把内容推出视口。
- [x] 编辑页输入框与 `AppSelect` 从 32px 调整为 38px，并优化紧凑选择器内边距；高 DPI 下文字基线、短选项和中文内容不再裁切，详情动态值允许换行。
- [x] 新增通用 `AppMultiSelect<T>`：使用锚定弹出层承载分类搜索、多选、已选计数、清空和完成操作；按钮显示已选摘要，点击完成、再次点击、Esc 或失焦均可关闭。
- [x] 面试方案编辑器已移除原始 JavaFX 多选 `ListView`，改用 `AppMultiSelect<String>`，加载旧方案、保存分类、右侧摘要及启动面试时的分类快照逻辑保持一致。
- [x] JavaFX 回归：`JavaFxFxmlLoadTest` 21 项通过，覆盖弹层打开、两项选择后摘要更新和关闭；完整 `mvnw test` 共 170 项，165 项通过、5 项按条件跳过，0 失败、0 错误。

本轮视觉证据：`plan-workspace-fixes-final.png`、`plan-editor-fixes-final.png`、`plan-detail-fixes-final.png`、`plan-knowledge-popup-fixes-final.png`、`compare-plan-list-overflow-fix.png` 与 `compare-plan-editor-popup-fix.png`，均位于 `C:\Users\35975\.codex\visualizations\2026\07\18\019f7443-ad60-7703-91ed-cad90a055040`。
